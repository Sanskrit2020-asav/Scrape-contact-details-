flask>=3.0
pypdf>=4.0
python-docx>=1.1

# Google Drive sync (itinerary_planner.sync_drive)
google-api-python-client>=2.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# Free local semantic search (optional but recommended)
sentence-transformers>=3.0

# Claude refinement layer (optional — needs ANTHROPIC_API_KEY)
anthropic>=0.69
