# Core platform runs on the Python standard library alone.
# Everything below is OPTIONAL — install only the features you need.

# --- LLM (richer emails & answers) ---
anthropic>=0.40.0

# --- API server (Feature 8: admin dashboard over HTTP) ---
fastapi>=0.110.0
uvicorn>=0.27.0

# --- Document ingestion for the knowledge base ---
pypdf>=4.0.0          # .pdf
python-docx>=1.1.0    # .docx
openpyxl>=3.1.0       # .xlsx pricing + knowledge

# --- Tests ---
pytest>=8.0.0

# --- Social Engagement Agent (social_agent/) ---
# No runtime dependencies: it uses the Python standard library only, and talks
# to the OpenAI and Apify REST APIs over urllib. `pytest` above runs its tests.
# For production the dashboard is a WSGI app; serve it with any WSGI server:
# gunicorn>=21.0.0
